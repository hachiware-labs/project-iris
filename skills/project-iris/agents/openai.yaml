interface:
  display_name: "Project Iris"
  short_description: "PM向け日次プロジェクトレポート"
  brand_color: "#4F46E5"
  default_prompt: "Use $project-iris to create today's PM-facing project report with main progress, focus issues, and evidence."

policy:
  allow_implicit_invocation: true
